ecommerce_profile:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account-json
      project: evocative-depot-427007-m8
      dataset: online_ecommerce_star_schema
      threads: 1
      # dbt will read the entire JSON layout straight out of memory
      keyfile_json: "{{ env_var('DBT_RAW_GCP_JSON') }}"