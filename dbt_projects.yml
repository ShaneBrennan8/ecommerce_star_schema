name: 'online_ecommerce_star_schema'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_profile'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  online_ecommerce_star_schema:
    staging:
      +materialized: view
      +schema: online_ecommerce_star_schema
    marts:
      +materialized: table
      +schema: online_ecommerce_star_schema