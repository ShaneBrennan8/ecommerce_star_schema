name: dbt_ci_cd

on:
  push:
    branches:
      - main
  pull_request:
    branches: [ main ]

env:
  DBT_PROJECT_DIR: ./
  DBT_PROFILES_DIR: ./

jobs:
  dbt_ci_cd:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9.x'

    - name: Install dbt
      run: |
        python -m pip install --upgrade pip
        pip install dbt-core==1.7.18 dbt-bigquery==1.7.9

    - name: Run dbt pipeline
      env:
        # 1. Feed the Base64 string safely into the environment
        BASE64_GCP_KEY: ${{ secrets.DBT_GCP_JSON_KEY_FILE }}
      run: |
        # 2. Decode it into a concrete file on the fly
        echo "$BASE64_GCP_KEY" | base64 --decode > ./gcp_key.json
        
        # 3. Run dbt pointing directly to that concrete file path
        dbt debug --target ci
        dbt deps
        dbt seed --target ci
        dbt run --target ci

    - name: Test dbt models
      env:
        DBT_GCP_JSON_KEY_FILE: ${{ secrets.DBT_GCP_JSON_KEY_FILE }}
      run: |
        dbt test --target ci