name: 'online_ecommerce_star_schema'
version: '1.0.0'
config-version: 2

profile: 'shane_bigquery_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  online_ecommerce_star_schema:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table